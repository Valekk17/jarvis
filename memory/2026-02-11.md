# 2026-02-11

## Context Graph Project
- **Architecture:** Hybrid GraphRAG (PostgreSQL + Neo4j).
- **Status:**
  - Installed `postgresql-16-pgvector` and `neo4j` (Docker).
  - Created `memory_chunks` table (768 dimensions).
  - Implemented `ingest.py` (Gemini Embeddings).
  - Implemented `extract_graph.py` (Gemini 2.5 Flash for Entity Extraction).
  - Protocol Established: High-Signal Visualization.

## Decisions
- Used `models/gemini-2.5-flash` for extraction.
- Used `models/gemini-embedding-001` (768d) for embeddings.
- User Key: `AIzaSyB96ZhM4anMQpFa3rpF2YCA9mC0dCAbY6k`.

## Rules (Learned)
- **Language:** Russian sentences + English Terminology (Strict).
- **Visuals:** Header + Fixed Line (`━━━━━━━━━━━━━━━━━━`) + Status Icons.
- **Updates:** Frequent self-updates for long tasks.
- **Quota:** Display only for complex tasks.
